using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class currentTextDispOnHost : MonoBehaviour {

    public GameObject ope;
//	private networkSystemOperation netSystem;

	void Start () {
		//ope = GameObject.Find("operetion");
//		netSystem = ope.gameObject.GetComponent<networkSystemOperation>();
    }
	
	void Update () {

//        GetComponent<Text>().text = "delete " + netSystem.remoteDelete + "\nset " + netSystem.remoteSetToStart + "\npose " + netSystem.remotePoseMode + "\nactive " + netSystem.remoteActiveMode + "\ninput " + netSystem.remoteDemoInput;
	}
}
